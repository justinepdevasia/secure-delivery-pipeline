# The .NET 10 SDK dropped VSTest support for Microsoft.Testing.Platform projects.
# xunit.v3 ships its own MTP runner, so `dotnet test` is pointed at it here.
[dotnet.test:runner]
name = "Microsoft.Testing.Platform"
